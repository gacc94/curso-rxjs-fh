/**
 *  1   Metodo Join() =>se utiliza en arreglos para combinar todos los elementos en
 *  una sola cadena de texto, separados por un delimitador especificado. Retorna la
 *  cadena resultante que contiene los elementos del arreglo concatenados.*/

const isUnion = numbers.join(',')

console.log(isUnion);
