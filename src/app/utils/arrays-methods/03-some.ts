/**
 *  1   Metodo Some() =>
 *      El método some() se utiliza en arreglos para verificar si al menos uno de los
 *  elementos cumple con una determinada condición. Retorna true si al menos un
 *  elemento cumple la condición, de lo contrario, retorna false..*/


const isSome = numbers.some((num: number) => {
    return (num % 2 === 0);
})

new Intl.ListFormat('es').format([]);
console.log(isSome)